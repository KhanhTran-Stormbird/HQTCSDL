<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản Lý Kho Hàng</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">

    <!-- Navbar -->
    <nav class="bg-blue-600 p-4 text-white flex justify-between items-center shadow-md">
        <div class="text-xl font-bold">
            <button onclick="location.href='/'" class="text-white">
                Quản Lý Kho Hàng
            </button>
        </div>
        <button onclick="location.href='/login'" class="bg-blue-800 hover:bg-blue-700 px-4 py-2 rounded text-white">
            Login
        </button>
    </nav>

    <!-- Main Content -->
    <div class="container mx-auto mt-8">
        <h1 class="text-2xl font-bold text-center text-gray-700 mb-6">Danh sách sản phẩm tồn kho</h1>
        <div class="overflow-x-auto">
            <table class="min-w-full bg-white border border-gray-200 rounded-lg">
                <thead>
                    <tr class="bg-blue-100 text-left text-gray-700">
                        <th class="py-3 px-4 border-b">#</th>
                        <th class="py-3 px-4 border-b">Tên sản phẩm</th>
                        <th class="py-3 px-4 border-b">Số lượng tồn</th>
                        <th class="py-3 px-4 border-b">Giá</th>
                        <th class="py-3 px-4 border-b">Hành động</th>
                    </tr>
                </thead>
                <tbody>
                    <% products.forEach((product, index)=> { %>
                        <tr class="hover:bg-gray-50">
                            <td class="py-2 px-4 border-b">
                                <%= index + 1 %>
                            </td>
                            <td class="py-2 px-4 border-b">
                                <%= product.TenSanPham %>
                            </td>
                            <td class="py-2 px-4 border-b">
                                <%= product.SoLuongTon %>
                            </td>
                            <td class="py-2 px-4 border-b">
                                <%= product.Gia %>
                            </td>
                            <td class="py-2 px-4 border-b">
                                <a href="/edit/<%= product.MaSanPham %>"
                                    class="text-blue-600 hover:underline mr-2">Sửa</a>
                                <a href="/delete/<%= product.MaSanPham %>" class="text-red-600 hover:underline">Xóa</a>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </div>
        <div class="mt-4 text-center">
            <a href="/add" class="bg-green-600 hover:bg-green-500 text-white px-6 py-2 rounded shadow-md">Thêm sản phẩm
                mới</a>
        </div>
    </div>

</body>

</html>