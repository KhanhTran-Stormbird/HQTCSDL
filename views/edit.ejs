<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sửa sản phẩm</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
    <div class="container mx-auto p-6 max-w-lg bg-white rounded-lg shadow-md">
        <h1 class="text-3xl font-bold mb-6 text-center">Sửa sản phẩm</h1>
        <form action="/edit/<%= product.MaSanPham %>" method="POST">
            <div class="mb-4">
                <label for="name" class="block text-lg font-medium">Tên sản phẩm:</label>
                <input type="text" id="name" name="name" value="<%= product.TenSanPham %>"
                    class="w-full p-3 border border-gray-300 rounded-lg" required>
            </div>

            <div class="mb-4">
                <label for="quantity" class="block text-lg font-medium">Số lượng tồn:</label>
                <input type="number" id="quantity" name="quantity" value="<%= product.SoLuongTon %>"
                    class="w-full p-3 border border-gray-300 rounded-lg" required>
            </div>

            <div class="mb-4">
                <label for="price" class="block text-lg font-medium">Giá:</label>
                <input type="number" id="price" name="price" value="<%= product.Gia %>"
                    class="w-full p-3 border border-gray-300 rounded-lg" required>
            </div>

            <div class="flex justify-between">
                <button type="submit" class="bg-yellow-500 text-white px-6 py-2 rounded-lg hover:bg-yellow-600">Cập nhật
                    sản phẩm</button>
                <a href="/" class="text-blue-500 hover:text-blue-600">Quay lại</a>
            </div>
        </form>
    </div>
</body>

</html>